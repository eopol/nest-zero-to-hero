{
  "name": "@nest-zero-to-hero/repository",
  "private": true,
  "packageManager": "pnpm@9.5.0",
  "description": "🚀 A modern high-performance monorepo project with pnpm + turbo + changeset + vite!",
  "type": "module",
  "keywords": [
    "monorepo",
    "pnpm",
    "turborepo",
    "changesets",
    "vite",
    "vitest",
    "tsup",
    "esbuild"
  ],
  "license": "MIT",
  "author": "i7eo",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "postinstall": "simple-git-hooks",
    "dev": "turbo run dev --parallel --continue",
    "dev:document": "turbo run dev --filter=\"./apps/document\"",
    "dev:playground": "turbo run dev --filter=\"./apps/playground\"",
    "build": "turbo run build",
    "build:packages": "turbo run build --filter=\"./packages/**\"",
    "build:node": "turbo run build --filter=\"./packages/node/**\"",
    "build:web": "turbo run build --filter=\"./packages/web/**\"",
    "build:document": "turbo run build --filter=\"./apps/document\"",
    "build:playground": "turbo run build --filter=\"./apps/playground\"",
    "update:packages": "pnpm up \"@page/*\" && pnpm up \"@unconfig/*\" && pnpm up \"@admin/*\" && pnpm up \"@client/*\"",
    "test": "turbo run test",
    "clean": "run-s \"clean:**\"",
    "clean:workspace": "turbo run clean",
    "clean:workspace:cache": "turbo run clean:cache",
    "clean:root-cache": "rimraf .turbo && rimraf pnpm-lock.yaml && rimraf node_modules",
    "lint": "turbo run lint",
    "format": "turbo run lint:prettier",
    "commit": "czg",
    "changeset:init": "pnpm exec changeset init",
    "prerelease": "tsx ./scripts/changesets/manual-generate-prereleases.cts",
    "changelog:gen": "tsx ./scripts/changesets/changelog-generate.cts",
    "changelog:write": "tsx ./scripts/changesets/changelog-write.cts",
    "changeset:publish": "tsx ./scripts/changesets/generate-releases.cts",
    "changeset:gen": "changeset"
  },
  "devDependencies": {
    "@types/jsdom": "^21.1.7",
    "@types/node": "^22.5.0",
    "@vitest/ui": "^2.0.5",
    "jsdom": "^25.0.0",
    "npm-run-all2": "^6.2.2",
    "rimraf": "^6.0.1",
    "tsx": "4.19.0",
    "turbo": "^2.0.14",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "pnpm": {
    "peerDependencyRules": {
      "ignoreMissing": [
        "eslint",
        "@algolia/client-search",
        "search-insights"
      ]
    }
  }
}
